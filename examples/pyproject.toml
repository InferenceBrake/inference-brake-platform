[project]
name = "inferencebrake-examples"
version = "0.1.0"
description = "Runnable InferenceBrake integration examples"
requires-python = ">=3.10"
dependencies = [
    "inferencebrake>=0.6.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
langchain = ["langchain-core>=0.1.0"]

[tool.uv]
package = false
